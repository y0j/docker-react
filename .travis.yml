sudo: required
services:
  - docker

before_install:
  - docker build -t y0j/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true y0j/docker-react npm run test

deploy:
  provider: elasticbeanstalk
  region: "eu-west-2"
  app: "docker"
  env: "Docker-env"
  bucket_name: "elasticbeanstalk-eu-west-2-567391046316"
  bucket_path: "docker"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: "$AWS_SECRET_KEY"